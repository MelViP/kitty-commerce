const cardsProductos = document.querySelectorAll('.galeria__productos_items');
const verProductos = document.querySelectorAll('.galeria__productos--link');

cardsProductos.forEach((item) => {
    item.addEventListener('hover', () => {
        
    });
});